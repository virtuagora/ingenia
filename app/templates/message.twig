{# set show_footer = true #}
{# set show_navbar = false #}
{# set body_class = '' #}

{% extends 'master.twig' %}

{% block body %}
  <a href="{{path_for('showHome')}}" target="_blank">
    <img alt="" class="error-logo-float" src="{{ asset('img/v-g.svg') }}">
  </a>
  <section class="hero is-primary is-fullheight">
    <div class="hero-body">
      <div class="container has-text-centered">
        <div class="columns">
          <div class="column is-6 is-offset-3">
              <i class="fa fas fa-info-circle fa-3x"></i><br>
            <h4>{{message}}</h4><br>
            <a class="button is-white is-outlined" href="{{path_for('showHome')}}">
              <i class="fas fa-home"></i>&nbsp;&nbsp;Ir al inicio</a>
          </div>
        </div>
      </div>
    </div>
  </section>
{% endblock %}